[project]
name = "sms-modem"
version = "0.1.0"
description = "SMS monitor for Strong Rising SEW132 USB modem"
requires-python = ">=3.13.5"
dependencies = ["pyusb>=1.3.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
sms-modem = "sms_modem.cli:main"
